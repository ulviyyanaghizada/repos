
@{
    ViewData["Title"] = "Index";
}
@model ICollection<PFeature>
<div class="container">
	<div class="card mt-5">
		<div class="table-responsive">
			<table class="table table-striped table-bordered">
				<thead>
					<tr>
						<th>Id</th>
						<th>Name</th>
						<th colspan="2">Operation</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var item in Model)
					{
						<tr>
							<td>@item.Id</td>
							<td>@item.Title</td>
							<td><a class="btn btn-danger" asp-area="Manage" asp-controller="PFeature" asp-action="Delete" asp-route-id="@item.Id">Delete</a></td>
							<td><a class="btn btn-info" asp-area="Manage" asp-controller="PFeature" asp-action="Update" asp-route-id="@item.Id">Update</a></td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>


